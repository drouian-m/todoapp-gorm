{
  "name": "root",
  "private": true,
  "scripts": {
    "frontend:run": "lerna run start --scope=@todoapp/frontend",
    "api:run": "cd packages/api && go run main.go"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
